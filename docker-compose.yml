version: '3'

# networks:
#   default:
#     external:
#       name: ceres-dev

services:
  tiler:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - ENV=development
    volumes:
      - .:/srv/tiler
    ports:
      - 8888:80
      - 9229:9229
    working_dir: /srv/tiler
    command: npm run dev
    env_file: .env
    # depends_on:
    #   - redis

  # # This is only used by the  download middleware
  # redis:
  #   image: redis:latest